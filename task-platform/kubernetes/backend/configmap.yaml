apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: taskapp
data:
  DB_HOST: "postgres.taskapp.svc.cluster.local"
  DB_PORT: "5432"
  DB_NAME: "taskdb"
  DB_USER: "postgres"
  PORT: "3000"
